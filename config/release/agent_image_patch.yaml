apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: porter-agent
spec:
  template:
    spec:
      containers:
        # Change the value of image field below to your controller image URL
        - image: chaunceyjiang/porter-agent:v0.4.2.5
          name: porter-agent
